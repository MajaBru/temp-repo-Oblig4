// const express = require("express");
// const router = express.Router();
// const schemaController = require("../controller/schemaController");

// // Route to save a new schema
// router.post("/", schemaController.saveSchema);

// // Route to fetch a schema by ID
// router.get("/:id", schemaController.getSchemaById);

// module.exports = router;
// //
